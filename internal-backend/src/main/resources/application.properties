spring.application.name=internal-backend
spring.config.import=optional:file:../.env[.properties]
server.port=${SERVER_PORT:8080}

# Supabase REST API Configuration
supabase.url=${NEXT_PUBLIC_SUPABASE_URL:${SUPABASE_URL:}}
supabase.anon.key=${NEXT_PUBLIC_SUPABASE_ANON_KEY}
# Required for certification DB rows, CV metadata, and PostgREST retries when RLS returns 403.
supabase.service.role.key=${SUPABASE_SERVICE_ROLE_KEY:}
supabase.storage.bucket=${SUPABASE_STORAGE_BUCKET:student-private-files}
# Must match Storage bucket id exactly; bucket should be Public so /object/public/... URLs in studentprofile.photo work.
supabase.storage.profile-bucket=${SUPABASE_STORAGE_PROFILE_BUCKET:user-profile-photo}
# Set true after running sql/alter-studentprofile-banner-cover.sql (adds banner_title + cover_url).
# Default true so banner headline saves; set SUPABASE_PROFILE_BANNER_COVER_COLUMNS=false if those columns are missing.
supabase.profile.banner-cover-columns=${SUPABASE_PROFILE_BANNER_COVER_COLUMNS:true}
<<<<<<< HEAD
=======
# PPA study-field assignments: join = table ppa_studyfield (default). column = studyfield.ppa_id only (no join table).
supabase.ppa.field-link=${SUPABASE_PPA_FIELD_LINK:join}
# useraccount.password is NOT NULL in some DBs; PPAs sign in via Supabase Auth, so this is only a DB placeholder.
supabase.ppa.useraccount-password-placeholder=${SUPABASE_PPA_USERACCOUNT_PASSWORD_PLACEHOLDER:__SUPABASE_AUTH__}
# After creating a PPA, call Supabase Auth invite (requires service role + configured SMTP in Supabase).
supabase.invite.on-ppa-create=${SUPABASE_INVITE_ON_PPA_CREATE:true}
# Base URL of the Next app (no trailing slash). Used as invite redirect_to …/auth/set-password; add that URL in Supabase Auth redirect allow list.
app.frontend.url=${FRONTEND_URL:}
# DB column for opportunity title (default title). Set titttle if your table still uses the typo.
supabase.opportunity.title-column=${SUPABASE_OPPORTUNITY_TITLE_COLUMN:title}
>>>>>>> 291958e6928042c894a84b4950699fb2ccd7f8cf
app.file.encryption.key=${APP_FILE_ENCRYPTION_KEY:}
spring.servlet.multipart.max-file-size=6MB
spring.servlet.multipart.max-request-size=6MB
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration